import { OcultarNumeroPipe } from './ocultar-numero.pipe';

describe('OcultarNumeroPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarNumeroPipe();
    expect(pipe).toBeTruthy();
  });
});
