import { OcultarCorreoPipe } from './ocultar-correo.pipe';

describe('OcultarCorreoPipe', () => {
  it('create an instance', () => {
    const pipe = new OcultarCorreoPipe();
    expect(pipe).toBeTruthy();
  });
});
